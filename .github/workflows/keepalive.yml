name: Keep Alive
on:
  schedule:
    - cron: '*/30 * * * *'  # 每30分钟（更温和）
  workflow_dispatch:        # 允许手动触发
jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: |
          curl -f https://your-app.streamlit.app || echo "App might be sleeping"
